#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running pre-commit checks..."
npm run format:check || exit 1
npm run lint || exit 1
# MDX lint is available via `npm run lint:mdx` but excluded here to avoid false negatives
